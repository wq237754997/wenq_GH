/* 
// 插件模版
(function($) {
    var defaults = {

    };

    $.tab = function(options){
        var options = $.extend(defaults,options);
        return this.each(function(){ // 对每个元素进行操作

        });
    };
})(jQuery);
*/
